using System;

class random
{
    static void Main()
    {
        Random rand = new Random();

        for (int i = 0; i < 9; i++)
        {
            int randomNumber = rand.Next(100, 201);
            Console.Write("{0} ", randomNumber);
        }
        Console.WriteLine();
    }
}
